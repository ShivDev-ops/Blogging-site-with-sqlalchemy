# Blogging Site With SQLAlchemy

This folder is for project notes and setup documentation.

## Notes

- `config.json` is hidden and ignored by git because it contains local configuration values.
- The Flask app loads settings from `config.json` at startup.
